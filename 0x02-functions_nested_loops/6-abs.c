#include "main.h"

/**
 * _abs - computes the absolute
 * @r: the variable to be checked
 * Description: this program computes the absolute
 *
 * Return: (0)
 */
int _abs(int r)
{
	if (r >= 0)
		return (r);
	else
		return (r * -1);
}
